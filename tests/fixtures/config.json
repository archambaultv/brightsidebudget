{
    "journal_path": "journal.xlsx",
    "backup_dir": "../output/backups",
    "check_config": {
        "forbidden_accounts_for_txns": ["Non classé"],
        "verify_balance_assertions": true
    },
    "export_config": {
        "export_path": "../output/exported_journal.xlsx"
    },
    "import_config": {},
    "rewrite_config": {}
}